# README

rails new workspace
cd workspace
git add -A
git commit -m " rils new workspace"
git push

########################################

update gem
bundle update
bundle install
rails g scaffold email object:string body:string 

###########################################""
